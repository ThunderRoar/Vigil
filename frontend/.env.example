# Elasticsearch ENDPOINT (Serverless) - the ES URL, not the Kibana URL.
# Find it under your project's "Connection details". Looks like:
# https://my-project-abc123.es.us-central1.gcp.elastic.cloud
ELASTIC_URL=
ELASTIC_API_KEY=

# Agent Service URL found after deloyment on Cloud Run. 
AGENT_URL=